# Mobile Architecture

The mobile application follows the same product and API concepts as the web application.

## Principles

- Reuse backend contracts.
- Keep platform-specific UI inside the mobile application.
- Keep shared domain definitions in the shared layer where appropriate.
- Avoid duplicating business rules.
- Keep authentication and API handling consistent with the web client.

Implementation details should be updated as the React Native application evolves.
